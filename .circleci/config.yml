version: 2.1
jobs:
  build:
    filters:
      branches:
        only:
          - master

orbs:
  maven: circleci/maven@1.0.3

workflows:
  maven_test:
    jobs:
      - maven/test # checkout, build, test, and upload test results
